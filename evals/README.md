# Evaluations

Evaluations protect the product from confident but unsupported advice. Every agent mode should have cases covering valid responses, incomplete context, conflicting evidence, unsupported platform claims, misleading account data, and requests for unapproved live-account changes.

`response-contract-regressions.json` is the initial machine-readable regression catalog. The Python unit tests are the executable enforcement layer for this first contract.
